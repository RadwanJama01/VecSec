# Core dependencies
# Using compatible versions - config.py has fallback for InMemoryVectorStore
# Note: langgraph 0.2+ requires langchain-core 0.2+ which includes pydantic_v1
langchain>=0.1.20
langchain-core>=0.2.0  # Updated for langgraph compatibility (includes pydantic_v1)
langchain-openai>=0.1.7
langchain-chroma>=0.1.3  # ChromaDB integration for vector stores
langgraph>=0.2.0  # Updated to work with langchain-core 0.2+ (includes pydantic_v1 support)
python-dotenv>=1.0.1
chromadb>=0.5.3
requests>=2.32.3

# NumPy compatibility fix (NumPy 2.x breaks scipy/scikit-learn/numba)
# Using NumPy 1.x for compatibility with sentence-transformers and scientific stack
numpy>=1.24.0,<3.0.0

# Embeddings (local, no API needed)
sentence-transformers>=2.2.0

# Metrics and monitoring
prometheus-client>=0.21.0

# --- Dev / CI dependencies ---
# Testing
pytest>=8.0.0
pytest-cov>=5.0.0  # Coverage reporting

# Code quality
ruff>=0.4.0  # Fast Python linter
mypy>=1.7.0  # Static type checker


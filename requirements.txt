# Core dependencies
# Using compatible versions - config.py has fallback for InMemoryVectorStore
# Updated to langchain 1.0.x series for compatibility
langchain>=1.0.0
langchain-core>=1.0.0  # Updated for langchain 1.0.x compatibility
langchain-openai>=1.0.0
langchain-chroma>=1.0.0  # ChromaDB integration for vector stores
langchain-community>=0.3.0  # Updated to be compatible with langchain-core 1.0.x
langchain-text-splitters>=0.3.0  # Updated to be compatible with langchain-core 1.0.x
langgraph>=1.0.0  # Updated to work with langchain-core 1.0.x
python-dotenv>=1.0.1
chromadb>=1.3.0
requests>=2.32.3

# NumPy compatibility fix (NumPy 2.x breaks scipy/scikit-learn/numba)
# Using NumPy 1.x for compatibility with sentence-transformers and scientific stack
numpy>=1.24.0,<3.0.0

# Embeddings (local, no API needed)
sentence-transformers>=2.2.0

# Metrics and monitoring
prometheus-client>=0.21.0

# --- Dev / CI dependencies ---
# Testing
pytest>=8.0.0
pytest-cov>=5.0.0  # Coverage reporting

# Code quality
ruff>=0.4.0  # Fast Python linter
mypy>=1.7.0  # Static type checker

# Optional dependencies (not required for VecSec core functionality)
# These may have version conflicts if installed - only install if needed:
# - streamlit: Not used in VecSec, conflicts with newer protobuf/packaging
# - datasets: Not used in VecSec, conflicts with newer fsspec
# - google-cloud-firestore: Not used in VecSec, conflicts with protobuf 6.x
# If you need these, use compatible versions or separate virtual environments

